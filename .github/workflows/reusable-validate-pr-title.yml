name: Reusable / Validate PR title

# Reusable (workflow_call) version of the estate's conventional-PR-title check.
#
# Dual merge policy (A-1176 / A-1175 / A-1177): feature / ship PRs use merge
# commits; release-please version PRs and fan-out PRs stay squash. Both
# allow_merge_commit and allow_squash_merge remain on (A-1177).
#
# This workflow still lints the PR title as a Conventional Commit — useful for
# humans, send-it, and squash merges where the title becomes the trunk subject.
# Under merge commits the PR title is NOT the sole bump signal: release-please
# reads the commit history that lands on trunk, so per-commit Conventional
# Commits (reusable-validate-commits.yml) are the load-bearing gate.
#
# The `pull_request` trigger stays in the CALLER stub; include `edited` so the
# check re-runs when a title is corrected (the default set omits it).
#
# STATUS-CHECK CONTEXT (A-400 / A-405): the job below is named
# `Validate PR title is a Conventional Commit` — the estate-canonical string a
# single required-check rule can pin across every repo. A reusable-workflow
# check renders as `<caller-job-id> / <job-name>`, so callers MUST use a
# consistent caller job id. This estate uses `pr-title`, giving the stable
# context `pr-title / Validate PR title is a Conventional Commit`. Do NOT rename
# either half.
#
# REQUIRED CALLER PERMISSIONS (A-621): a called reusable workflow's job cannot be
# granted more than the caller's GITHUB_TOKEN holds. Under the org default
# `default_workflow_permissions: read`, a caller with no `permissions:` block
# hands this workflow a read-only token, the `pr-title` job below can't get the
# `pull-requests: read` it needs, and the run dies at compile time with a
# `startup_failure` — no jobs, no logs, and the REST API hides the reason (`gh run
# view` only offers a generic hint; read the one-line error on the Actions UI run
# page). So the caller MUST declare the scope this job needs.
#
# Consume from another repo (floating major tag `@v1` — see README):
#
#   permissions:
#     pull-requests: read
#   jobs:
#     pr-title:
#       uses: rheged-studio/shared-workflows/.github/workflows/reusable-validate-pr-title.yml@v1

on:
  workflow_call:
    inputs:
      types:
        description: >-
          Newline-separated Conventional Commit types to allow. Defaults to the
          types /send-it emits across the estate.
        type: string
        required: false
        default: |
          feat
          fix
          docs
          chore
          ci
          refactor
          perf
          test
          build
          style
          revert

# Deny all token scopes at the workflow root; the job below grants only the
# pull-requests:read it needs (least privilege; zizmor-clean).
permissions: {}

jobs:
  pr-title:
    name: Validate PR title is a Conventional Commit
    runs-on: ubuntu-latest
    permissions:
      pull-requests: read
    steps:
      - name: 🔠 Lint PR title
        uses: amannn/action-semantic-pull-request@48f256284bd46cdaab1048c3721360e808335d50 # v6.1.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          # Only feat / fix / a `!` breaking marker are release-triggering
          # (release-please default node bump table); the rest are non-release.
          types: ${{ inputs.types }}
